// Given const prices = [200, 450, 100, 800], use .map() to return a new array where each price has a 10% discount applied.


const prices = [200, 450, 100, 800];

const prizes = prices.map(price => price - price * 0.1);
console.log(prizes);
